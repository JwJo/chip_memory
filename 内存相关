PC计数器：program counter,存储内存中下一条指令所在的地址，随后执行该指令。

“向量”的意思就是跳转到哪个地址，所以这里的“复位向量”就是指单片机复位之后会跳转的哪个地址，
通常是0x00地址。与此类似的还有“中断向量”，指的是中断发生后，程序指针会跳转到哪个地址。


复位向量是单片机的启动地址
中断向量：中断服务程序的入口地址。在某些计算机中，中断向量的位置存放一条跳转到中断服务程序入口地址的跳转指令

“向量”的意思就是跳转到哪个地址，所以这里的“复位向量”就是指单片机复位之后会程序指针跳转的哪个地址，通常是0x00地址。
与此类似的还有“中断向量”，指的是中断发生后，程序指针会跳转到哪个地址
